# Model artifacts — copy manually to bundled_models/ before deploying
# On HF Spaces, track these with Git LFS instead
bundled_models/*.onnx
bundled_models/*.ubj
bundled_models/*.joblib

# Runtime
__pycache__/
*.pyc
scans.db
*.db
.env
